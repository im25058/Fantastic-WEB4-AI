package fantastic_ai;

interface ResearchAgent {

    // User sends a research query
    getResearchReport(query: string) -> string

    // Verify that a report hash exists on blockchain
    verifyReport(hash: string) -> bool

    // Retrieve audit log
    getAuditLog(execution_id: string) -> string
}
package fantastic_ai;

interface ResearchAgent {

    runResearch(query: string) -> string

    verifyReport(hash: string) -> bool

}